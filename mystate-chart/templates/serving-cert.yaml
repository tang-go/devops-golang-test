apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "mystate-chart.fullname" . }}-serving-cert
  labels:
  {{- include "mystate-chart.labels" . | nindent 4 }}
spec:
  dnsNames:
  - '{{ include "mystate-chart.fullname" . }}-webhook-service.{{ .Release.Namespace
    }}.svc'
  - '{{ include "mystate-chart.fullname" . }}-webhook-service.{{ .Release.Namespace
    }}.svc.{{ .Values.kubernetesClusterDomain }}'
  issuerRef:
    kind: Issuer
    name: '{{ include "mystate-chart.fullname" . }}-selfsigned-issuer'
  secretName: webhook-server-cert